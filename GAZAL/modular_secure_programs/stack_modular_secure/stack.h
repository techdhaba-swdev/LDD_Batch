#include <stdbool.h> // Include the header file for the bool data type

// Function prototypes
void push(int value); // Declare a function prototype for push, which adds an element to the stack
int pop(); // Declare a function prototype for pop, which removes and returns an element from the stack
bool stack_is_empty(); // Declare a function prototype for stack_is_empty, which checks if the stack is empty
bool stack_is_full(); // Declare a function prototype for stack_is_full, which checks if the stack is full