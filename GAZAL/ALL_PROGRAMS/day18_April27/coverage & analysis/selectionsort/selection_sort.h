#ifndef SELECTION_SORT_H
#define SELECTION_SORT_H

#include <stddef.h> // Include standard library for size_t

void selection_sort(int arr[], size_t n); // Function prototype for selection sort

#endif
