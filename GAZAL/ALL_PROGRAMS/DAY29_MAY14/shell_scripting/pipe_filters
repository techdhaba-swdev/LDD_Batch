#!/bin/bash
#Task 5: pipes and filters
echo "enter the file name"
read file_name

echo " enter word in file you wanted to be sorted nd find occurrences"
read  sort_word

#pipes output to sort nd finding occurences
grep "$sort_word" "$file_name" | sort
