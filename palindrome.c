
#include<stdio.h>
typedef struct{
	int arra[MAX];
	int max;
	int size;
}
LIST
 void insert ( int  j, int it,LIST *pl)
	int i;
	for(i=pl->size;i>=j;'i=i-1)
{
	pl->arr[i+1]=pl->arr[i];
};
p1->arr[j]=it;
p1->size = p1->size +1;
}
