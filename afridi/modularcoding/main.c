#include "main.h" // Including the header file which presumably contains the function declarations

int main() {
    // Function calls to various add and sub functions
    // Assuming these functions are defined in "main.h" and implemented elsewhere
    // Printing the result of each function call along with a description

    printf(" add is %d", add2(4, 5)); // Call to add2 function with arguments 4 and 5
    printf(" add is %d", add3(10, 20, 30)); // Call to add3 function with arguments 10, 20, and 30
    printf(" add is %d", add4(10, 20, 30, 40)); // Call to add4 function with arguments 10, 20, 30, and 40

    printf(" sub is %d", sub2(14, 5)); // Call to sub2 function with arguments 14 and 5
    printf(" sub is %d", sub3(40, 20, 20)); // Call to sub3 function with arguments 40, 20, and 20
    printf(" sub is %d", sub4(70, 20, 10, 30)); // Call to sub4 function with arguments 70, 20, 10, and 30

    return 0; // Return statement indicating successful completion of the program
}


