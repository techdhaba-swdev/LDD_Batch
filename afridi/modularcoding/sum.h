#ifndef SUM_H
#define SUM_H

#include "linked_list.h" // Include the linked list module to access its structures

// Function prototype for calculating the sum of elements in the list
int calculateSum(struct Node* head);

#endif
