#ifndef FACTORIAL_H // it is a preproccessor called as if not defined and it checks that macro has defined in the code.and guardheader file from being included multiple times
#define FACTORIAL_H// it is a preprocessor that defines macro.
int factorial(int n);//here function factorial is declared  and allow other source file to use this function
#endif// it preproccessor indicate end of conditional compilation block
