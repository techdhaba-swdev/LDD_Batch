#ifndef CAL_H_
#define CAL_H_
#include"add1.h"  // includes the files inside the add1.h
#include"sub1.h"  // includes the files inside the sub1.h
#include"mul1.h"  // includes the files inside the mul1.h
#include"div1.h"  // includes the files inside the div1.h
#endif 
