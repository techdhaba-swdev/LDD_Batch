// Node.h

#ifndef NODE_H
#define NODE_H

// Structure for a Node in the stack.
typedef struct Node {
    int data; 
    struct Node* next; // Pointer to the next node in the stack.
} Node;

#endif // NODE_H


