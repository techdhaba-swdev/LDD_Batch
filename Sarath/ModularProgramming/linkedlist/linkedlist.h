//created header guard to prevent multiple inclusion of same header files and ensuring code is compiled only once
#ifndef LINKEDLIST_H_  
#define LINKEDLIST_H_

struct node; //Declared the structure 
void insert_at_start(int data); //Declaration of API to insert at start
void insert_at_end(int data); //Declaration of API to insert at end
void print(void); //Declaration of API to print

#endif
