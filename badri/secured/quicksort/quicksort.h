#ifndef QUICKSORT_H   //Checks if the header file quicksort.h is not defined.
#define QUICKSORT_H // defines quicksort.h to prevent multiple inclusions of the header

// Function declaration for quicksort
void quicksort(int arr[], int low, int high);

#endif  // Ends the conditional inclusion and ensures that the contents of the header file are only included once in the compilation process.
