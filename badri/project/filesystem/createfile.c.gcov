        -:    0:Source:createfile.c
        -:    1:#include "header.h"
function createFile called 1 returned 100% blocks executed 100%
        1:    2:struct File* createFile(const char* name) { // Define a function to create a new file with the given name.
        1:    3:    struct File* newFile = (struct File*)malloc(sizeof(struct File)); // Allocate memory for the new file.
        1:    4:    strcpy(newFile->name, name); // Copy the name of the file to the newly created file structure.
        1:    5:    newFile->next = NULL; // Initialize the next pointer to NULL.
        1:    6:    return newFile; // Return a pointer to the newly created file.
        -:    7:}
