#ifndef ORDER_H //it is headergauard for order.h
#define ORDER_H //it ensures that order.h is included one time

#include "header.h" // it includes header.h headerfile

void inOrder(struct Node *root);//function for inorder 

#endif
