#ifndef NODE_H // ifndef defines the headerguard
#define NODE_H // it ensures the node.h is included one time 

#include "header.h"// include the header.h contains headers like stdio.h and stdlib.h

struct Node* createnode(int data);// function for creating new node

#endif//ends the ifndef header guard
