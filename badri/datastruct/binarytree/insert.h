#ifndef INSERT_H // it is headergauard for insert.h
#define INSERT_H // ensures that headerguard is included one time

#include "header.h"// includes header.h headerfile

struct Node *insert(struct Node *root , int data); // function for inserting data into nodes

#endif//end of ifndef header gauard
